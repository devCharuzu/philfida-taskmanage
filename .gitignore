.DS_Store

# Build output / deps (were previously committed)
taskflow-app/node_modules/
taskflow-app/dist/

# Env files — never commit, at any level
.env
.env.local
.env.*.local

# test scaffold — seeds a session from a URL param, must never ship
taskflow-app/public/__seed.html
